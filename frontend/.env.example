# Leave empty in dev so API calls use the Vite proxy (/api → backend on your PC).
# Required for ngrok + phone testing — do NOT set to http://localhost:5000.
# VITE_API_URL=

# Optional: set to your ngrok HTTPS URL while testing on mobile data, e.g.
# CLIENT_URL=https://abc123.ngrok-free.app
